# Check Folder Disk Space with Good UI in Linux

Install **ncdu**:

```
sudo apt install ncdu -y
```

Then:

```
sudo ncdu -x /
```

This gives you an interactive view of exactly where your **GB's** are being used.